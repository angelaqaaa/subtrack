# SubTrack Backend Environment Configuration

# Database Configuration
# These four are the only variables the app reads. src/Config/database.php
# parses this file at startup and nothing else looks at the environment.
DB_HOST=localhost
DB_NAME=subtrack_db
DB_USER=your_username
DB_PASS=your_password
